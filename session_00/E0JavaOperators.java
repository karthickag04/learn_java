public class E0JavaOperators {
    
}

// Basic Operators:
// Arithmetic: +, -, *, /, %
// Assignment: =, +=, -=, *=, /=, %=
// Comparison: ==, !=, >, <, (30>=10 && 10<=10 && 10!=20)
// Logical: &&, ||, !

// Bitwise: &, |, ^, ~, <<, >>, >>>


// Arithmetic Operators
// + (Addition) - e.g., a + b
// - (Subtraction) - e.g., a - b
// * (Multiplication) - e.g., a * b
// / (Division) - e.g., a / b
// % (Modulus) - e.g., a % b
// ++ (Increment) - e.g., a++
// -- (Decrement) - e.g., a--

// AssignmentOperators
// = (Assignment) - e.g., a = b
// += (Addition Assignment) - e.g., a += b    a = 10, b = 20 == >  -- addwithassign
// -= (Subtraction Assignment) - e.g., a -= b == >> a = a - b; 
// *= (Multiplication Assignment) - e.g., a *= b  == >> a = a * b;
// /= (Division Assignment) - e.g., a /= b
// %= (Modulus Assignment) - e.g., a %= b

// Comparison Operators
// == (Equal to) - e.g., a == b  equalCheck()
// != (Not equal to) - e.g., a != b 
// > (Greater than) - e.g., a > b
// < (Less than) - e.g., a < b
// >= (Greater than or equal to) - e.g., a >= b
// <= (Less than or equal to) - e.g., a <= b

// Logical Operators
// && (Logical and) - e.g., a && b
// || (Logical or) - e.g., a || b
// ! (Logical not) - e.g., !a


// Bitwise Operators
// & (Bitwise and) - e.g., a & b
// | (Bitwise or) - e.g., a | b
// ^ (Bitwise xor) - e.g., a ^ b
// ~ (Bitwise not) - e.g., ~a
// << (Left shift) - e.g., a << b
// >> (Right shift) - e.g., a >> b
// >>> (Unsigned right shift) - e.g., a >>> b


// 00110010
// 00110011
// --------
// 00110010

// // Converts a letter to its decimal (ASCII) representation as a String
// public static String letterToDecimal(char letter) {
//     int decimalValue = letter;
//     return String.valueOf(decimalValue);
// }

// // Converts a decimal number to its binary representation as a String
// public static String decimalToBinary(int decimal) {
//     return Integer.toBinaryString(decimal);
// }
//  string method to convert letter to decimal and decimal to binary
